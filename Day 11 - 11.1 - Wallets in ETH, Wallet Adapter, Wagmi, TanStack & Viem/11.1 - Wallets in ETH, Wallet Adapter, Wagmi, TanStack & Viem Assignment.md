# 11.1 - Wallets in ETH, Wallet Adapter, Wagmi, TanStack & Viem — Assignment

> **Notes**: https://feather-lion-ff2.notion.site/Week-11-1-12f246afc9f280c4a4cdf4dbf9144104
> **Wagmi Docs**: https://wagmi.sh
> **Viem Docs**: https://viem.sh

---

## Core Assignment

### Task 1 — Conceptual Questions

Answer in your own words:

1. What is the difference between a **hot wallet** and a **cold wallet**? When would you use each?
2. Why should you **never share your private key or seed phrase** — even with wallet support staff?
3. What is the purpose of a **wallet adapter**? Why not connect directly to MetaMask's API?
4. What does Wagmi do that you'd otherwise have to build manually?
5. What is the difference between **Viem** and **Wagmi**? (Hint: one is low-level, one is React hooks)
6. Why does Wagmi use **TanStack Query** internally? What problem does it solve?
7. What happens if you lose your seed phrase but still have your device?

---

### Task 2 — Build a Wagmi ETH DApp

Set up a full Wagmi + Viem + TanStack React app:

```bash
npm create vite@latest my-eth-dapp -- --template react-ts
cd my-eth-dapp
npm install wagmi viem@2.x @tanstack/react-query
```

Build a DApp with these features:
1. **Connect Wallet button** — using `useConnect` + `injected()` connector
2. **Show connected address** — using `useAccount`
3. **Show ETH balance** — using `useBalance`
4. **Send ETH form** — input recipient + amount, submit via `useSendTransaction`
5. **Disconnect button** — using `useDisconnect`

All should work on **Sepolia testnet** (get test ETH from https://sepoliafaucet.com).

---

### Task 3 — Viem Direct Calls

Without using Wagmi hooks, use **Viem directly** to:

```ts
import { createPublicClient, http, formatEther } from 'viem'
import { mainnet } from 'viem/chains'

const client = createPublicClient({
  chain: mainnet,
  transport: http()
})

// 1. Fetch ETH balance of Vitalik's address
const balance = await client.getBalance({
  address: '0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045'
})
console.log('Vitalik balance:', formatEther(balance), 'ETH')

// 2. Get the latest block number
const blockNumber = await client.getBlockNumber()
console.log('Latest block:', blockNumber)

// 3. Get transaction count (nonce) of an address
const txCount = await client.getTransactionCount({
  address: '0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045'
})
console.log('Transaction count:', txCount)
```

---

## Bonus Challenges

### Challenge 1 — Multi-Wallet Connector

Add support for multiple wallet connectors:
- MetaMask (injected)
- Coinbase Wallet
- WalletConnect

Show all available connectors as buttons. Display which one is currently connected.

### Challenge 2 — Token Balance Checker

Build a component that:
- Takes any ERC-20 token contract address as input
- Uses `useReadContract` + the ERC-20 ABI
- Displays token name, symbol, and user's balance

### Challenge 3 — Transaction History

Use Viem's `getLogs` to fetch recent Transfer events for an ERC-20 token and display them in a table using TanStack Table.

---

## References

- Wagmi Docs: https://wagmi.sh/react/getting-started
- Viem Docs: https://viem.sh/docs/getting-started
- TanStack Query: https://tanstack.com/query/latest
- Sepolia Faucet: https://sepoliafaucet.com
- Wagmi Examples: https://wagmi.sh/examples/connect-wallet
